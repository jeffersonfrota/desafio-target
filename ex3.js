let indice = 12;
let soma = 0;
let k = 1;

while (k < indice) {
    k = k + 1;
    soma = soma + k;
 
}

console.log(soma);
// O resultado sera 77
// Caso a ordem das expressoes no laco fossem alteradas , teriamos um resultado diferente.
// Mas considerando o cenario da questão, passa a passo como foi posto, o resultado é 77.